const HOME = "/";

// Add [pathname]: menuItemKey to render active key
const rootMenuKeys: { [k: string]: string } = {
  "/": HOME,
};

const Routes = {
  HOME,
  rootMenuKeys,
};

export default Routes;
